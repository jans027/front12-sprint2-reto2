import styled from "styled-components";
import Button from '@mui/material/Button';

export const BotonesUl = styled.ul`
        width: 100%;
        margin: auto;
        border: 1px solid red;
        il{

        }



`
export const Container1 = styled.section`
        margin: 6em auto;
        display: flex;
        flex-wrap: wrap;
        justify-content: center;
        p{
                width: 100%;
                text-align: center;
                padding: 1em;
        }
`
export const BootstrapButton = styled(Button)`
        line-height: 1em;
        width: 60%;
        margin: 1em;
        padding: 2em;
        padding: 0 1em 1.5em;
`

export const Button1 = styled(Button)`
        line-height: 1em;
        width: 80%;
        margin: auto;
        padding: 0 1em 1.5em;
`